---
title: "GoScoot QA 實戰演練學習筆記"
date: 2026-05-06
draft: false
category: "學習筆記"
---

# GoScoot QA 實戰演練學習筆記

## 專案背景

GoScoot 智慧共享滑板車系統，包含使用者 App、車輛 IoT 端、與後台 Web 系統的完整 QA 測試演練。

---

## 第一階段：需求分析與測試案例設計 (Test Case Design)

### 功能：低電量強制鎖車邏輯
- 電量 ≤ 15%：降速模式（25km/h → 10km/h）
- 電量 ≤ 5%：強制鎖車
- IoT 每 10 秒回傳一次電量

### 學習瓶頸與問題

- 只想到「有沒有觸發」，沒有想到要具體定義前置條件、操作步驟、預期結果
- Edge Case 只想到「太早觸發」與「沒觸發」，遺漏了時間差、硬體限制、Race Condition

### 解決辦法

- Test Case 要具體到：前置條件 + 操作步驟 + 預期結果，三者缺一不可
- 建立 Edge Case checklist 思維：時間差（10秒 polling 間隔）、硬體誤差（感測器 ±2%）、並發競爭（解鎖與鎖車指令同時到達）

### 主管反饋重點

- **等價類劃分**：將電量分成三個行為區間（正常/降速/鎖車），每區間取一個代表值測試
- **邊界值分析**：重點測 16%、15%、14% 和 6%、5%、4% 這六個值，工程師差一個等號行為就不同
- 強制鎖車是「漸停」還是「硬停」？這是人身安全議題，規格不清楚要去找 PM 釐清

---

## 第二階段：API 測試與異常處理 (API Testing)

### API：POST /api/v1/vehicle/telemetry

### 學習瓶頸與問題

- 只測了 `battery` 一個欄位，忽略其他 5 個欄位（gps、speed、timestamp、vehicle_id、status）
- 不知道如何模擬網路延遲、封包遺失、惡意注入
- 環境問題：PowerShell 的 `curl` 是 `Invoke-WebRequest` 的別名，參數語法不同，造成多次失敗

### 解決辦法

- 每個 API 欄位都是獨立的攻擊面，需逐一設計異常值
- 用 Python `requests` 寫測試腳本取代手打 curl，避免環境差異問題
- JSON 測資存成獨立檔案，或直接用程式碼管理

### 主管反饋重點

- **發現真實 Bug**：TC09 SQL Injection 測試 FAIL，vehicle_id 驗證只檢查「是否為非空字串」，未驗證格式（應為 `GS-\d{5}`），導致惡意字串通過驗證拿到 200 OK
- **兩層防禦缺一不可**：API 層做格式驗證（Regex）、資料庫層用 Parameterized Query
- **環境差異本身就是 QA 問題**：Windows 測過的指令放到 Linux CI 不一定能跑，要用程式化腳本統一

### 測試結果

修正 vehicle_id 格式驗證後，12/12 全部通過。

---

## 第三階段：Bug 報告與缺陷管理 (Bug Reporting & Root Cause Analysis)

### 情境：用戶在 101 地下停車場結束騎乘後收到 NT$4,380 帳單（正常約 NT$65）

### 學習瓶頸與問題

- Bug Report 格式不完整：缺少 Severity/Priority 定義與理由、環境資訊、重現步驟、Log 需求
- 調查策略停在「GPS 不穩」的現象層，沒有往計費邏輯深挖
- 忽略「NT$4,380 這個數字本身」作為線索

### 解決辦法

**標準 Bug Report 欄位：**
標題 / Severity + Priority（附理由）/ 環境資訊 / 重現步驟 / 實際結果 vs 預期結果 / 附件與 Log 需求

**Root Cause 兩個假說：**
- 假說 A：結束騎乘 API 因無訊號送出失敗，後台計時器未停止，持續計費至系統超時
- 假說 B：IoT 模組回傳 null 或跳點 GPS，計費系統用異常座標算出天文距離

### 主管反饋重點

- **Log 先於現場**：先查後台 ride_end timestamp、IoT telemetry log、計費 log，確認假說後再去現場重現
- **Bug Report 目的**：不是描述你看到什麼，而是讓別人能在 30 分鐘內重現你看到的
- **數字是最好的線索**：NT$4,380 ÷ NT$65 ≈ 67 倍，直接排除「距離算錯一點點」的可能性

---

## 第四階段：自動化與效能測試策略 (Automation & Performance)

### 情境：跨年夜 5,000 人同時在信義區掃碼解鎖

### 學習瓶頸與問題

- 通過標準描述不夠具體（「回應時間不能過高」≠ 工程標準）
- 未考慮地理集中造成的局部熱點問題

### 解決辦法

**通過標準必須數字化：**
- P95 回應時間 ≤ 3 秒
- 錯誤率 ≤ 0.5%
- 解鎖成功率 ≥ 99%
- 計費金額 100% 正確（抽驗 100 筆）

### 主管反饋重點

**適合自動化：** API Regression Test、計費邏輯、狀態機驗證、跨平台 Smoke Test

**不適合自動化：** 探索性測試、全新功能第一輪、實體硬體驗證、UX 評估

**壓測設計（k6）：**
- 分五階段：緩升 → 穩定 → 瞬間 Spike → 維持尖峰 → 觀察恢復

**關鍵盲點：座標集中問題**
5,000 人在同一地點，資料庫地理索引的「局部熱點」會讓該區域查詢爆炸，其他城市卻正常。
均勻分布的壓測找不到這個問題，需要特別設計座標集中在信義區的測試情境。

---

## 總結與心得

### 四個階段的核心收穫

| 階段 | 最重要的一件事 |
|------|--------------|
| 測試設計 | 邊界值是 Bug 最密集的地方，系統性思考優於直覺列舉 |
| API 測試 | 每個欄位都是攻擊面；發現問題比執行測試更有價值 |
| Bug 報告 | 數字是線索；Log 先於現場；報告目的是讓人能重現 |
| 效能測試 | 通過標準要數字化；考慮真實情境的特殊分布，不只是量 |

### 實際動手做的體驗

- 親手建立 Mock Server，用 Python 腳本執行 12 筆 API 測試
- 發現並修復一個真實的輸入驗證 Bug（TC09 SQL Injection）
- 體驗完整的 QA 循環：需求分析 → 測試設計 → 執行 → 發現缺陷 → 修復驗證
